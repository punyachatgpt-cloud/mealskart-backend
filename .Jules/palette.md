## 2025-06-03 - [Choice Button Accessibility]
**Learning:** Interactive toggle buttons should use `aria-pressed` to indicate their state to screen readers, and grouped options should be wrapped in an element with `role="group"` and a descriptive label.
**Action:** Always include `aria-pressed` on toggle-like buttons and use `role="group"` for logical sets of choices.
