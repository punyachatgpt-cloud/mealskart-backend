fastapi==0.128.8
uvicorn==0.46.0
httpx>=0.27.0

# ── Auth additions ────────────────────────────────────────────────────────────
supabase>=2.10.0              # Supabase Python client (Auth + Postgres)
python-jose[cryptography]>=3.3.0  # JWT decode / verify (Supabase uses HS256)
python-dotenv>=1.0.0          # Load .env for local dev (Render uses env vars natively)

# ── Push notifications ────────────────────────────────────────────────────────
pywebpush>=2.0.0              # Web Push / VAPID signing

# ── AI Chef ───────────────────────────────────────────────────────────────────
anthropic>=0.40.0             # Claude API — AI Chef chat endpoint
